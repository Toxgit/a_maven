//USEUNIT Settingsы
//USEUNIT Toolbar
//USEUNIT VariablesHelper
//USEUNIT OpenStrHelper

function ConstraintDialog()
{
  //initialization
  var testStrategyPath =  strLocation + "Constraint.strat";//путь к стратегии для теста
  var toolbar = new Toolbar.Toolbar();
  var variables = new Variables.Variables();
  var selectConstraint = new SelectConstraint.SelectConstraint();
  /*
  //precondition
  openStrByPath(testStrategyPath);
  toolbar.variables();*/
  
  //test bodys
  //Проверка shows all constraints that have the same datatype as the variable
   clickRowByName("String1");
   chooseProperty("Constraint name");
   variables.toSelectConstraint();
   listElementExists(selectConstraint, "Constraint1");
   listElementNotExists(selectConstraint, "Constraint0");
   //constraintExists("Constraint1");
   //constraintNotExists("Constraint0");
   selectConstraint.cancel();
   /*
  //postcondition
  variables.ok();
  closeOneStrWhithWarning();*/
  
}
