function WindowError()
{
  this.alias = Aliases.Maven.WindowError;
  
  this.text = function(){ 
    return this.alias.Text.wText;
  }
  this.close = function()  {
    this.alias.Close.ClickButton();
      
  }
 
}
function test(){
  var werror = new WindowError();
  q = werror.text();
  Log.Message(q)
}